# 05 — Verify the v0.2.0 candidate

**What to build:** Produce trustworthy completion evidence for the owner by verifying the finished Host Dashboard on supported Windows, checking its read-only and unelevated boundary, and aligning durable documentation with the behavior that actually shipped.

**Blocked by:** 04 — Complete the dashboard experience.

**Status:** ready-for-agent

**Verification:** complete — 2026-08-14

- [x] Formatting, solution build, and the complete automated Core, Windows, and presentation suite pass with results recorded as milestone evidence.
- [x] The narrow production-wiring smoke exercises the same read-only composition as the application and asserts report shape and stable check identity without requiring the test computer to be Ready.
- [x] A recorded supported-Windows UI smoke covers window launch, automatic diagnosis, the default folder, choosing another folder, stale-result indication, Refresh, visible progress, Cancel, independent Local/Tailscale rendering, details, keyboard operation, and closing during a run.
- [x] The UI smoke confirms that no UAC prompt appears and no setup, repair, share, credential, permission, firewall, mapping, installation, or public-access action is offered.
- [x] A safety review confirms that launch, Refresh, cancellation, probe failure, and window close stay unelevated, non-mutating, local-only, and free of secret or unnecessary metadata disclosure.
- [x] Product, preflight, architecture, testing, roadmap, and glossary language matches the verified implementation without rewriting the historical v0.1.0 contract.
- [x] The active roadmap records completed work, remaining work, exact build/test/UI-smoke evidence, and any unresolved blocker rather than implying completion from code alone.
- [x] The completion summary clearly tells the owner what was accomplished and what, if anything, remains before v0.2.0 can be accepted.
- [x] The milestone is marked Complete and the verified commit is tagged and pushed as `v0.2.0` only after explicit owner acceptance; otherwise it remains untagged with its evidence and outstanding decision recorded.

## Comments

- 2026-08-14 automated verification: `dotnet format BallsServer.slnx --verify-no-changes --no-restore` passed. `dotnet build BallsServer.slnx -c Release --no-restore` passed with 0 warnings and 0 errors. The focused production-wiring smoke passed 1 test, and `dotnet test BallsServer.slnx -c Release --no-build --no-restore` passed all 210 non-mutating tests: Core 126, Presentation 15, Windows 69.
- 2026-08-14 supported-Windows UI smoke: On an unelevated Windows Pro 25H2 build 26200.9168 session, the Release Host Dashboard launched without UAC, automatically diagnosed the profile Documents folder, selected another existing local folder through the standard picker, marked the prior snapshot as needing Refresh, rendered ordered progress and separate Local/Tailscale results, exposed bounded prerequisite details, and showed visible keyboard focus in the declared folder path, Browse, Refresh, and detail-toggle order. Cancel became available and was invoked during visible progress; this fast host completed the remaining checks before a canceled-state screenshot, while the deterministic presentation tests verified Canceled state, preserved snapshots, and late-result suppression. Closing during visible progress removed the dashboard window. No setup, repair, share, credential, permission, firewall, mapping, installation, elevation, public-access, telemetry, or export action appeared.
- 2026-08-14 safety review: The application manifest is `asInvoker`, the test shell was unelevated, and production composition exposes only launch, Refresh, Cancel, folder selection, detail expansion, and close. Windows access is limited to read-only token, version-registry, volume, ACL, service-status, three enum-allow-listed PowerShell queries, and bounded local Tailscale-status observations. Static inspection found no write, elevation, setup/helper, share, ACL, firewall, mapping, credential, telemetry, upload, export, background timer, public-SMB, or guest-access path; error evidence is sanitized and raw child-process stderr/output is not displayed.
- 2026-08-14 verification checkpoint: Automated, production-wiring, safety, durable-document, and supported-Windows UI checks are complete. The milestone remains in Verification and untagged pending explicit owner acceptance and the subsequent tag decision.
- 2026-08-14 Spec review blocker (resolved below): Standards review passed without findings. Spec review found that the recorded keyboard smoke reached folder path, Browse, Refresh, and details, but did not prove keyboard focus and activation for the enabled active-run Cancel control. A disposable slow-run harness using the shipped WPF window and presentation module was prepared outside the repository, but Computer Use correctly refused to foreground it while the desktop remained under continuous user input. At that checkpoint, Ticket 05 remained open until that exact keyboard check could run with an idle desktop; no repository product code was implicated.
- 2026-08-14 blocker resolution: With the desktop idle, Computer Use foregrounded a disposable slow-service harness built outside the repository from the shipped `MainWindow`, `HostDashboardPresentation`, and application focus resources. During visible `Checking…` progress, Tab placed the visible keyboard focus indicator on enabled Cancel and Enter activated it. The window rendered `Canceled`, retained the Not checked snapshot, disabled Cancel, and re-enabled Refresh. The harness closed normally and was removed after the smoke. All Ticket 05 acceptance criteria are now verified; only explicit owner acceptance remains before milestone completion and the `v0.2.0` tag.
- 2026-08-14 owner acceptance: The owner explicitly accepted v0.2.0. The milestone is Complete, its verified completion commit is tagged and pushed as `v0.2.0`, and v0.3.0 setup security and architecture design is now the current milestone. This checkpoint does not authorize or implement setup mutation.
