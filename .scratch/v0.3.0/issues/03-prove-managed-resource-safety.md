# 03 — Prove managed-folder, share, and private-network resource safety

**What to build:** Define and prototype the narrow safety rules for the managed folder, descendant reparse points, product group, exact NTFS ACE, fixed share, compliant global SMB prerequisite, and separate private LAN and Tailscale firewall rules.

**Blocked by:** 01 — Publish the threat model and operation matrix.

**Status:** ready-for-agent

- [x] Missing, remote, device, drive-root, protected-system, non-NTFS, root/ancestor-reparse, and unresolved descendant-reparse folders fail closed without taking ownership or recursively rewriting children.
- [x] A disposable prototype proves descendant link discovery, retained folder/volume identity, time-of-check/time-of-use drift detection, and the refusal policy.
- [x] The exact product ACE is one Allow entry for the product-group SID with Modify and Synchronize, container/object inheritance, and no propagation restriction.
- [x] An in-memory or disposable-fixture prototype proves exact ACE round-trip, effective access, owner/control-flag preservation, unrelated-ACE multiset preservation, conflict refusal, and exact reversible removal.
- [x] The fixed `Balls` share and product access group use stable SID/object identity and never adopt or edit an unmanaged name, path, marker, permission, or equivalent object.
- [x] Share creation has no permissive intermediate state and verifies Administrators Full, product group Change, no Everyone/guest access, exact path, and compliant SMB policy.
- [x] Pure or guarded prototypes prove the LAN and Tailscale rule plans cannot create Public/Any/Any inbound TCP 445 exposure, cannot edit built-in/unmanaged rules, and can be re-observed exactly; inability to express or prove a narrow rule resolves to refusal.
- [x] Noncompliant SMB, Server-service, signing, policy-managed, interface, or firewall state produces exact administrator guidance rather than machine-wide mutation.
- [x] Development-host guards refuse every machine-wide prototype mutation; any mutating mode requires a disposable-VM marker and remains outside the default suite.
- [x] Prototype and guard tests are written test-first where applicable and pass without touching user data or production configuration.
- [x] Both review axes pass with every Critical and Important finding resolved.
- [x] The ticket and authoritative roadmap contain exact verification evidence, and the coherent checkpoint is committed and pushed with unrelated work preserved.

## Comments

Implementation checkpoint, 2026-08-14: published `docs/security/v0.3.0-managed-resource-safety.md` and an isolated model/test pair under `.scratch/v0.3.0/prototypes/` without adding either project to `BallsServer.slnx` or `src/`. Genuine RED first failed because the requested project and folder/ACE/share/firewall/guard APIs did not exist; self-review then captured a second RED for missing before/after effective-access preservation and canonical share fingerprint behavior. Focused GREEN passes exactly 81/81. The model proves fail-closed folder and descendant-link validation, retained volume/file/descriptor/link identity with TOCTOU refusal, exact reversible product ACE preservation, stable group/share/rule identity conflicts, atomic fixed-share permissions, observe-only SMB administrator handoffs, distinct private LAN/Tailscale TCP 445 plans, exact re-observation, and every later-VM guard denial. The only real fixture is one unique temporary directory and known non-secret file, deterministically removed. Ticket 01 verifier passes 39 operation rows/13 columns/86 stories/6 exits; Ticket 02 verifier passes 125/125; Ticket 03 verifier passes 81/81 plus scanner mutation/cleanup, production isolation, and secret/private-path output scans. Both isolated-project and solution formatting checks pass, all three Ticket 03 PowerShell files parse, Release build succeeds with 0 warnings/errors, and all 210 default tests pass: Core 126, Presentation 15, Windows 69. `git diff --check` passes. No live ACL, group, share, firewall, SMB, service, policy, interface, Tailscale, network, managed-folder, user-file, production, or VM mutation occurred. Native Windows round-trip and live mutation remain expected later disposable-VM evidence. Both independent review axes and the pushed-checkpoint closure remain open for the controller.

Review-fix round 1, 2026-08-14: adversarial RED first reproduced 8 failures across traversal containment, same-product-SID ACE conflicts, share/firewall self-attested ownership, nullable SMB dialect acceptance, and collapsed firewall handoffs; the desired typed contract then failed compilation on missing canonical/effective-access/ownership/authorization APIs, and the five-class verifier self-test failed because its recursive guard did not exist. The isolated model now requires canonical path segments plus exact target/root volume and stable target identity; an exactly-one same-SID ACE multiset in verify/idempotency/removal; fresh before/after effective access for product group, owner, SYSTEM, and Administrators; separate protected share/firewall ownership records; exact share plus fresh folder/ACE/effective/SMB/limited-grant/guest-denial intersection; every immutable firewall expression field; fail-closed complete SMB dialect bounds; and distinct typed administrator guidance. The verifier now positively checks the exact project dependency/composition boundary, recursively scans source and built IL, and mutation-tests nested production references, source adapters, project dependencies, linked compile items, and IL evidence with deterministic cleanup. Focused GREEN and Ticket 03 verification pass exactly 162/162. Full final gate evidence is recorded in the implementation report. No native ACL/share/firewall/group/network or VM mutation, production composition, or unmanaged adoption occurred. Both independent review axes and the pushed-checkpoint closure remain open for the controller.

Review-fix round 2, 2026-08-14: adversarial RED first failed 14 of 176 tests for forged folder-containment decisions, contradictory share-success tuples, semantic CIDR/interface gaps, and undefined security enums. Additional self-review REDs covered blank stable evidence, invalid group/folder identities, malformed helper tokens, LAN host/subnet confusion, a throwing empty live share identity, and a caller-forged broad share plan. Final audits captured 2 targeted RED failures for IPv4/IPv6 private-address supernets and 3 targeted RED failures for rooted, duplicate-separator, and trailing-separator link-path aliases. The final folder boundary now recomputes canonical absolute link/target containment and exact volume identity from raw evidence, retains distinct link-object and target-object IDs, and refuses either identity drifting at use. Share authorization binds every exact result tuple and raw resource identity to one helper-owned token/epoch/context and canonical plan. IPv4/IPv6 CIDRs use canonical address/prefix math and exact freshly observed interface/private range binding. Undefined decision enums fail closed. The verifier recursively scans both project sources and composition boundaries, metadata-inspects both built assemblies, and compiles real nested adversarial fixtures for all five guard classes including actual P/Invoke before proving cleanup. Focused GREEN and Ticket 03 verification pass exactly 243/243. No native Windows or VM mutation and no production composition was added. Both independent review axes and the pushed-checkpoint closure remain open for the controller.

Review-fix round 3, 2026-08-14: adversarial RED failed 31 of 53 targeted cases for forged desired-state fields, contradictory retained-folder tuples, duplicate links, malformed shared tokens, and public Verified overclaims; the verifier RED exposed only five classes/four assemblies with no structural-before-build or dependency-closure result. One reusable closed validator now governs both share planning and accepted-plan verification: exact markers, canonical group/grant identity, closed folder identity, bounded positive revision/epoch, and a 32-lowercase-hex helper token suffix are mandatory. Folder use requires the entire canonical retained success tuple and unique independently valid links before TOCTOU comparison. Public output verifies only the exact success tuple; every contradictory or undefined result is Refused or Unknown. Before either project executes, the verifier rejects Target/UsingTask/Exec/build events and unapproved imports/Directory.Build files, evaluates both exact compile/package/project/reference graphs, and metadata-checks the local non-framework dependency closure against the approved package asset allowlist. The self-test never executes its structural canary, compiles a safe declaration-only imported P/Invoke dependency, proves the exact `DependencyMutation.dll|pinvoke:Mutate` finding, and cleans every unique fixture. Focused GREEN and Ticket 03 verification pass exactly 296/296. No native Windows, production, system, network, or VM mutation occurred. Both independent review axes and pushed-checkpoint closure remain open.

Controller closure, 2026-08-14: final re-review found leading-zero SID aliases/reused stable identities, relative-link separator/order aliases, and compiler-executed Analyzer plus legacy XML-namespace build inputs outside the pre-evaluation verifier graph. Controller-owned RED failed 6 of 59 focused cases, while the strengthened verifier expectation failed because the old result exposed only seven classes/six assemblies. Canonical SID decimal round-trip, same-machine/distinct-RID and stable-object rules, exact forward-slash relative paths, discovery-sort order, and exact public-result strings are now pinned. A namespace-free positive XML allowlist rejects every unapproved project/root-props property, item, attribute, build/response/user file, analyzer, and ambient build input before MSBuild; evaluated inputs then require the exact implicit SDK analyzers and compiler graph. Nine self-test classes across seven real assemblies include executable analyzer and namespaced-build canaries that never create a sentinel, plus exact transitive P/Invoke detection and cleanup. Targeted GREEN passes 59/59 and Ticket 03 passes 302/302. Ticket 01 remains 39 rows/13 properties/86 stories/6 exits; Ticket 02 remains 125/125; all three Task 03 scripts parse; prototype/test/solution formats pass; Release build is 0 warnings/errors; default tests are Core 126, Presentation 15, Windows 69 (210 total); `git diff --check` and residue checks pass. No system, network, native, production, user-data, or VM mutation occurred. Both independent closure reviews and pushed-checkpoint evidence remain open.

Final exact-input closure, 2026-08-14: closure re-review found that seven root-props entries could substitute a duplicate allowed key for one missing required key, and that nested analyzer configuration was accepted by basename. The new pre-evaluation self-tests omit each required root property while duplicating another and add a nested policy override; all refuse before evaluation, restore cleanly, and leave no fixture. Approved `.editorconfig` paths are exactly the worktree/Git-common copies with matching normalized content, while nested/ambient `.editorconfig` or existing `.globalconfig` refuses. The self-test now proves eleven classes across seven assemblies. Ticket 03 remains 302/302; no native, production, system, network, user-data, or VM mutation occurred. Both independent final closure reviews and pushed-checkpoint evidence remain open.

Ordinal analyzer-policy closure, 2026-08-14: specification/security review reproduced that PowerShell's default unique sort treated two approved `.editorconfig` contents differing only by case as identical. The strengthened self-test was RED with 10/11 detected classes and both case-drift fields false while cleanup remained complete. Exact normalized content now uses ordinal, case-sensitive string equality; the approved case-only drift fixture refuses before any project evaluation, the same eleven-class clean restoration passes, and no sentinel or residue survives. Ticket 01 passes 39 rows/13 properties/86 stories/6 exits, Ticket 02 passes 125/125, and Ticket 03 passes 302/302. All three Task 03 PowerShell files parse; prototype/test/solution formatting passes; Release build is 0 warnings/errors; the default Core 126, Presentation 15, and Windows 69 tests pass (210/210); diff, sentinel, and temporary-residue checks are clean. Final review and pushed-checkpoint evidence remain open.

Completion review gate, 2026-08-14: specification/security review PASS and standards review PASS on ordinal-closure commit `422b5f29790befb6e19fd639bfe59ec5cdc5dbf1`; both reported zero Critical, Important, or Minor findings. The reviewers independently replayed the approved-path case-only analyzer-policy drift, confirmed ordinal rejection before project evaluation, restored identical content to a passing evaluated baseline, required the new public self-test fields, and verified eleven detected/eleven clean guards with deterministic cleanup. Fresh independent evidence also passed Ticket 01, Ticket 02 125/125, Ticket 03 302/302, PowerShell parsing, all formats, Release 0 warnings/errors, default 210/210, diff, status, sentinel, and residue checks. Review evidence is committed; only push, fresh fetch, and exact local/upstream parity proof remain open. No production, native Windows, system, network, user-data, or VM mutation is claimed.

Synchronized checkpoint, 2026-08-14: the complete reviewed Ticket 03 sequence was pushed through `30297ca66a101de74a9bcaf1bf7d7b44f9ba4e77`. A fresh fetch then proved local `HEAD` and `origin/codex/v0.3.0` both equal that SHA, divergence `0 0`, and a clean tracked worktree. Only this synchronization record's commit/push and the final closure record remain before the combined checkpoint criterion can be marked complete.

Pushed-checkpoint closure, 2026-08-14: synchronization evidence commit `3080229ea57e66784cc5742c876632498e637f35` was pushed, then a fresh fetch proved local `HEAD` and `origin/codex/v0.3.0` both equal that SHA, divergence `0 0`, and a clean tracked worktree. Both review axes are clean, every acceptance criterion is complete, exact verification evidence is authoritative, and Ticket 03 is closed. The milestone remains In progress with Ticket 04 next; no merge, owner acceptance, milestone completion, or tag is claimed.
