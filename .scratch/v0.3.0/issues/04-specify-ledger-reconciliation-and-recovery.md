# 04 — Specify ledger, reconciliation, rollback, and recovery

**What to build:** Produce the protected product-owned ledger, transaction, reconciliation, rollback, removal, audit, recovery-manifest, and total-loss design that makes every later repair narrow and evidence-based.

**Blocked by:** 02 — Prove the privileged-helper authorization boundary; 03 — Prove managed-folder, share, and private-network resource safety.

**Status:** ready-for-agent

- [x] The ledger schema records version, host/owner identity, revision, desired-state fingerprint, exact stable resource identities/fingerprints, endpoint snapshots, operation journal, and non-secret audit references while explicitly excluding secrets and password-derived identifiers.
- [x] Protected primary/mirror state, append journal, atomic replacement, revision advancement, schema migration refusal, restrictive ACL, and current-user client-intent boundaries are exact.
- [x] The operation state machine covers preview, revalidation, consent, Planned, Started, primitive verification, cancellation, rollback, Repair needed, recovery, completion, and idempotent replay.
- [x] Reconciliation deterministically classifies every resource as OwnedConformant, OwnedDrifted, Missing, UnmanagedConflict, Ambiguous, or Unknown using the stable proof defined by the owning operation.
- [x] Crash injection before and after every primitive and atomic revision boundary proves the documented recovery result.
- [x] Stale revision, corrupt primary, corrupt mirror, total ledger loss, drift, missing object, unmanaged conflict, ambiguity, access denial, policy override, and unknown observation have exact automated or manual outcomes.
- [x] Rollback removes only an unchanged object created by the current transaction; repeated setup/removal converges without duplicates and not-found is success only where defined.
- [x] Total ledger loss makes product automation read-only, produces a non-authoritative object-by-object recovery manifest/order, and never deletes the managed folder or user files.
- [x] Revoked-grant tombstones and local audit evidence follow the approved 90-day non-secret retention rule.
- [x] A platform-neutral in-memory prototype and test-first crash/reconciliation/property matrix pass, including secret/redaction scans.
- [x] Both review axes pass with every Critical and Important finding resolved.
- [x] The ticket and authoritative roadmap contain exact verification evidence, and the coherent checkpoint is committed and pushed with unrelated work preserved.

## Comments

Implementation checkpoint, 2026-08-14: published `docs/security/v0.3.0-ledger-reconciliation-recovery.md`, linked it from the threat model and operation matrix, and added an isolated in-memory model/test pair under `.scratch/v0.3.0/prototypes/ledger-recovery*` plus `scripts/Verify-V030Task04.ps1`. The exact schema preserves host/owner/revision/desired-state, Ticket 03 stable resource proofs, endpoints, journaled Ticket 02 binding fingerprints, and non-secret audit/tombstone references while making every ledger, journal, manifest, and current-user copy non-authoritative for privileged mutation. Protected primary/mirror/journal paths, restrictive SYSTEM/Administrators/write and recorded-owner/read ACL, atomic replacement/revision order, schema-1-only refusal, operation transitions, all six reconciliation classes, exact rollback/removal/convergence rules, 11 named recovery outcomes, total-loss read-only manifest order, and exact 90-day boundary are fixed. Genuine RED/GREEN chronology is retained in `.scratch/v0.3.0/evidence/task-04-tdd.md`; focused GREEN passes 107/107, including all 32 ownership/live-observation combinations, actual in-memory crash injection at all 26 boundaries for a three-primitive plan, and total-loss classification even when its corroborating journal is also corrupt. Ticket 01 passes 39 rows/13 columns/86 stories/6 exits; Ticket 02 passes 125/125; Ticket 03 passes 302/302; Ticket 04 passes 107/107 with production isolation, secret/private-output, and residue checks. Prototype/test/solution formatting and all six Ticket 01-04 PowerShell parses pass. Release build succeeds with 0 warnings/errors and all 210 default tests pass: Core 126, Presentation 15, Windows 69. `git diff --check` passes. No production composition, elevation, IPC, native Windows adapter, live ACL/group/share/firewall/SMB/service/policy/credential/mapping mutation, managed-folder/user-file access, network operation, filesystem fixture, or mutating VM run occurred. Native Windows durability/rollback and disposable-VM mutation evidence remain intentionally deferred. Both independent review axes and the controller's pushed-checkpoint closure remain open.

Review-fix round 1 checkpoint, 2026-08-14: all consolidated Critical, Important, and Minor findings are resolved in the candidate. The schema validator is total and closed across every nested record and collection; canonical identity formats, recursive duplicate-key/credential-payload refusal, exact Task 02 protocol/transaction/context/sequence/hash/terminal journal binding, independently hashed protected ownership, complete rollback/replay/consent bindings, state-derived 26-point crash recovery, typed revision-exhaustion refusal, and protected-host/current-user type separation are executable. Total-loss observation/instruction enums keep uncertain items visible but non-actionable and exclude the managed folder; tombstone/audit/superseded retention has exact trusted-chain/time/access/reference/preservation/idempotence behavior. The decisive adversarial RED failed at compile with `CS0246` for the missing `RetentionCollections` hardened contract. Focused GREEN passes 120/120 after self-review regressions. `Test-V030Task04Verifier.ps1` and the public verifier require the reviewed Ticket 03 eleven-class real mutation result, reject exact project/compiler/analyzer/build/dependency deviations before execution, and inspect both built dependency closures for forbidden metadata/PInvoke. Independent re-review axes and controller commit/push/parity closure remain open and are not claimed.

Bounded closure round 2 checkpoint, 2026-08-14: the resource-operation map now exactly binds LAN/Tailscale firewall, grant, attributable session, verification creation, and verification cleanup to OP-10/11/12/16/32/38. Reconciliation validates desired evidence before `Missing` and carries protected/unowned/invalid provenance into convergence with no permissive default. Journals bind request revision `N` to committed ledger `N+1`, validate failed/unknown plus rollback/recovery progressions, and allow exactly one terminal, including a final repair-needed handoff or a later terminal after recovery. Crash state uses a closed ordered typed plan/journal; all 26 outcomes have independent expected values; malformed/reordered/extra records and `long.MaxValue` fail closed. Revision commit and idempotent completion require started, verified, and applied evidence for the entire exact planned primitive set. Weaker public crash/replay/copy/retention bypasses are removed or non-public, and the only public convergence entry point requires complete raw desired/ownership/live proof. Retention stores only closed record kind/ID/removal time/non-secret evidence fingerprint and preserves valid structured evidence identically. Targeted TDD RED/GREEN is retained; final round-2 GREEN is 24/24 and focused GREEN is 140/140. Both independent review axes and push/parity closure remain open and are not claimed.

Completion review gate, 2026-08-14: standards and specification/security reviewers each returned PASS on final bounded checkpoint `7fced7853e9a7c7e5f14f329afc16a4e713720c4`. Their yes/no closure checks independently confirmed full-plan commit coverage, removal of the weak convergence overload, complete raw proof on the sole public convergence path, and accurate 24/24 targeted plus 140/140 focused evidence. All earlier acceptance findings are closed; only commit/push/fetch/parity evidence remains.

Pushed-checkpoint closure, 2026-08-14: the complete reviewed Ticket 04 sequence and review evidence were pushed through `b5ba60238b9d4fa47f311b6eaf00806fc5d1d210`. Fresh fetch proved local `HEAD` and `origin/codex/v0.3.0` equal that SHA, divergence `0 0`, and a clean tracked worktree. Ticket 04 is closed; Ticket 05 is next.
