# 05 — Validate explicit endpoint identity and switching

**What to build:** Complete the product-host identity and separate LAN/Tailscale endpoint decision with durable prototype evidence showing that selection, drift, switching, mapping replacement, credential targets, and IP diagnostics never become silent fallback.

**Blocked by:** 01 — Publish the threat model and operation matrix.

**Status:** ready-for-agent

- [x] Product host identity is independent from the Windows name, MagicDNS name, IP address, display label, credential target, and mapped UNC root.
- [x] LAN and Tailscale observations are timestamped separate endpoints; one exact selected root drives authentication, mapping, and verification, while the provider-supported server credential target is proven and recorded separately from the full mapping UNC.
- [x] The initial setup code contains exactly one selected endpoint and no alternate; any later change arrives only in a separate minimal owner-transferred endpoint-update bundle containing one new selected endpoint and matching the client's existing product host ID, grant ID, and credential revision.
- [x] Endpoint-update import is explicit and followed by preview and exact-root re-verification; the bundle performs no discovery, guessing, remote pairing, credential transfer, automatic selection, or automatic switch.
- [x] All seven guided scenarios and every illegal transition pass the durable verifier preserved by the retained remote reference `origin/prototype/v0.3-endpoint-switching` at commit `012a424ba2d8ce23ada4f2b527a2404bbe28d5c0`, or equivalent immutable artifact/verifier/result evidence is copied into the milestone branch before the remote reference is removed.
- [x] Selected-path failure preserves current client state and performs no alternate discovery or authentication; only an explicitly imported owner-transferred update can enable Switch, which previews the new UNC root, mapping replacement, separately proven provider credential-target change, and one verification attempt.
- [x] IP literal behavior remains transport-diagnostic-only and cannot become saved, mapped, authenticated, or reported as verified access.
- [x] Windows/Tailscale name drift or collision suffix preserves existing client state, marks the endpoint stale, and requires re-observation and explicit verification.
- [x] The verifier proves the fail-closed state-machine invariants, endpoint-update import/revision refusals, and the presence of all owner-visible controls without network, credential, or drive mutation.
- [x] Computer Use/browser walkthrough results are recorded honestly: current private local-file URL policy denied interactive inspection, no alternate workaround is used, and no visual-pass claim is made.
- [x] Endpoint research, specification, glossary, ADR, and roadmap language agree without inventing aliases, renames, or automatic fallback.
- [x] Both review axes pass with every Critical and Important finding resolved.
- [x] The ticket and authoritative roadmap contain exact verification evidence, and the coherent checkpoint is committed and pushed with unrelated work preserved.

## Comments

Completion review gate, 2026-08-14: the durable milestone-local endpoint artifact closes Windows CRLF portability and the final endpoint-update binding criteria. Bounded standards and specification/security reviews PASS final fix commit `da3734027285ef112916f8cab5f272a7d5d29c18`, confirming imported-update-only mapping replacement, endpoint-specific canonical provider-target proof, real canonical UTC validation, and exactly 13 actual owner-interactive controls. The verifier passes 7 guided scenarios, 11 refusal cases, and 13 controls; Tickets 01-04, Release build, default 210, formatting/parsers, isolation, diff, and residue gates remain green. Only review-evidence commit and push/fetch/parity remain.

Pushed-checkpoint closure, 2026-08-14: the reviewed Ticket 05 sequence and evidence were pushed through `04700f142b47a4da4473b94a2cdedec436537efa`; fresh fetch proved exact local/upstream equality and divergence `0 0`. Ticket 05 is closed with Ticket 06 next.

Implementation checkpoint, 2026-08-14: `origin/prototype/v0.3-endpoint-switching` still resolves exactly to `012a424ba2d8ce23ada4f2b527a2404bbe28d5c0`. Its three immutable artifacts were copied into the milestone branch because validation found two explicit durability gaps: extraction depended on LF-only markers, and the retained verifier predated the final endpoint-update/provider-target contract. The bounded milestone copy now accepts LF or CRLF and passes with `PASS: 7 guided scenarios, endpoint invariants, and 13 visible controls verified.` plus `PASS: 10 endpoint-update binding/refusal cases, separate provider-target proof, and CRLF/LF extraction verified.` It proves exact host/grant/share/credential-revision binding; refuses mismatches, secret/alternate/discovery/credential-target fields, IP roots, same-root updates, malformed generation times, unsupported and out-of-order transitions; preserves mapping, provider target, and attempt count on import/refusal; requires separate provider-target proof and explicit preview; rechecks drift, collision, and revision after import; and consumes exactly one simulated authentication attempt on Switch. LAN and Tailscale observations remain separately timestamped, names and product identity remain independent, and mapping UNC/provider target remain separate. Ticket 01 passes 39 operation rows, 13 completion columns, 86 stories, and 6 exit checks; Tickets 02-04 pass 125/125, 302/302, and 140/140. Computer Use/browser inspection was denied by private local-file URL policy; no alternate workaround and no visual-pass claim. No production code, network, credential, mapping, drive, browser, Windows, managed-folder, user-file, or VM mutation occurred. Both independent review axes and commit/push/fetch/parity closure remain open and are not claimed.

Bounded acceptance-fix checkpoint, 2026-08-14: corrected four overclaims in the initial implementation evidence and no other behavior. Targeted TDD reproduced that drift → re-observe → select → verify → Add mapping returned `Ready` and replaced the recorded root without an imported update; that arbitrary provider strings and the old LAN target were both accepted for a Tailscale switch; that canonical-looking but impossible `2026-99-99T99:99:99Z` was accepted; and that the 13-control count included title, headings, and prose. The model now refuses any `ADD_MAPPING` replacement, allowing replacement only through matching imported-update Switch; derives the canonical server-only provider target from the selected exact UNC and rejects arbitrary, share-qualified, or cross-endpoint targets; requires a real canonical UTC whole-second generation time; and marks/asserts exactly 13 actual owner-interactive controls while treating Clear and Reset as prototype helpers. Focused verification passes with `PASS: 7 guided scenarios, endpoint invariants, and 13 owner-interactive controls verified.` plus `PASS: 11 endpoint-update binding/refusal cases, separate provider-target proof, and CRLF/LF extraction verified.` Existing mapping, provider target, and authentication-attempt state remain unchanged across every new refusal. Independent review and push/fetch/parity closure remain open and are not claimed.
