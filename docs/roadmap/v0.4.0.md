# v0.4.0 — First Working Share

- **State:** Retired incomplete
- **Depends on:** v0.3.0

## Retirement

Work stopped on August 19, 2026 when active development moved to the broader
[Balls](https://github.com/scwlkr/balls) platform. This milestone was never accepted or tagged as
complete. Its unchecked verification and recovery requirements remain unchecked.

Two later local commits are preserved without merge under
`archive/v0.4.0-completion-wip`. The preservation branch is historical WIP, not completion
evidence or a supported release.

## Outcome

An owner and one approved client can use the same Balls Server application to create an authenticated share and access it as a persistent Windows drive through File Explorer over either LAN or Tailscale. The application may close after setup; the host computer must remain powered on and awake for access.

## In scope

- Present clear **Host Files** and **Connect to Files** actions in one application.
- Implement the smallest complete set of privileged operations approved in v0.3.0.
- Refuse to take over an existing unmanaged share or incompatible permissions.
- Create the managed share, product-owned permission groups, and minimum private-network firewall access.
- Enforce SMB 3.0+ with SMB1 disabled, with safe handling of detected compatibility conflicts.
- Create one limited access grant without using the owner's personal Windows credentials.
- Display a copyable setup code containing one explicit LAN or Tailscale endpoint and the limited credential.
- On the client, accept the setup code, save the credential after consent, map an available persistent drive, and verify create/read/rename/delete with one temporary file.
- Record product-owned host and client changes for safe **Stop Sharing** and **Disconnect** actions.
- Distribute one portable unsigned build through a public GitHub release; one PowerShell command installs or updates application files without replacing user or product state.
- Guide Tailscale installation or sign-in as a separate user-controlled handoff when that access path is selected.

## Out of scope

- Multiple clients, automatic peer discovery, silent updates, code signing, guest or blank-password access, public TCP 445, port forwarding, and deletion of user data.

## Completed work

- [x] Milestone outcome, dependency, and safety boundary approved.
- [x] Owner accepted the first vertical outcome: one application, one host computer, one client, explicit LAN/Tailscale paths, setup-code handoff, persistent File Explorer access, and UAC only for previewed host changes.
- [x] Owner accepted a portable unsigned prototype with a public GitHub install/update bootstrap before formal signing and installer work.
- [x] Vertical specification and four executable tickets published under `.scratch/v0.4.0/`.
- [x] Ticket 01 delivered the same-app Host Files and Connect to Files workflow, versioned setup-code contract, deterministic preview state, branded WPF views, and off-screen WPF rendering coverage.
- [x] Ticket 02 checkpoint delivered the typed one-use helper protocol, separate UAC helper and authoritative approval view, fixed stdin-only host mutation plan, rollback path, protected ledger plan, and one-time setup-code response without mutating the development computer.
- [x] Ticket 03 checkpoint delivered exact MagicDNS/hostname setup codes with host-qualified limited identities, consented native Credential Manager storage, one-attempt persistent drive mapping, isolated temporary-file verification, secret-free client state, and narrow Disconnect behavior.
- [x] Ticket 04 checkpoint delivered the self-contained win-x64 publisher, SHA-256 release sidecar, and one-line public GitHub install/update bootstrap with app-state preservation and Start Menu integration.

## Remaining work

- [ ] Implement host setup, client connection, persistent mapping, and basic stop/disconnect recovery.
- [ ] Implement the portable GitHub install/update bootstrap.
- [ ] Complete automated, isolated mutation, two-computer, and UI verification.

## Exit checks

- [ ] The same build presents Host Files and Connect to Files with the Balls Server identity and clear connecting progress.
- [ ] Previewed and consented host setup succeeds from a supported clean VM.
- [ ] A clean client uses the setup code and receives a reconnecting File Explorer drive over one selected private path.
- [ ] Both computers pass create/read/rename/delete round trips while the Balls Server windows are closed.
- [ ] Repeating setup is idempotent and verifies effective share plus NTFS access.
- [ ] Existing unmanaged configuration is preserved or produces a clear stop.
- [ ] Cancelled and failed setup leaves a known, recoverable state.
- [ ] Stop Sharing and Disconnect remove only recorded product-owned configuration and preserve the managed folder and its files.
- [ ] The one-line GitHub bootstrap installs and updates app files while preserving product state.
- [ ] Default tests remain unelevated/non-mutating; isolated mutation tests pass.
- [ ] Owner accepts the evidence and the completion commit is tagged `v0.4.0`.

## Evidence

- Scope acceleration decisions accepted by the owner on 2026-08-17: files persist but are unavailable while the host is off; the application may close after setup; the initial host is the owner's computer; one setup code connects the client; and v0.4.0 absorbs the minimum host, client, mapping, and update work required for the first usable share.
- Public-repository preparation on 2026-08-17: current files and complete private Git history scanned with Gitleaks 8.30.1; zero secret findings. Two private metadata fixtures were sanitized before the fresh public-history root. NuGet reported no known vulnerable packages from the configured source; Release build and all 210 automated tests passed.
- Ticket 01 verification on 2026-08-17: format verification and Release build passed with zero warnings or errors; all 215 automated tests passed. The WPF smoke test rendered both real compiled views off-screen on an STA thread, exercised both preview buttons, verified accessible names and the logo, and confirmed the limited credential password was absent from rendered text.
- Ticket 02 implementation checkpoint on 2026-08-17: format verification and Release build passed with zero warnings or errors; all 228 automated tests passed. Background checks rendered the real helper view off-screen, parsed the fixed PowerShell mutation script without executing it, verified private values stay out of command lines and diagnostics, and exercised strict request, response, setup-code, credential-generation, and bounded-pipe contracts. No Windows mutation ran on the development computer.
- Ticket 03 implementation checkpoint on 2026-08-17: format verification and Release build passed with zero warnings or errors; all 232 automated tests passed. Background checks exercised the compiled WPF connection controls plus deterministic native-boundary fakes for exact endpoint selection, credential consent, one mapping attempt, verification rollback, secret-free state, and Disconnect ownership. No client-side Windows mutation ran on the development computer.
- Ticket 04 packaging checkpoint on 2026-08-17: format verification and Release build passed with zero warnings or errors; all 233 automated tests passed. The self-contained win-x64 publisher produced a 74 MB package with app, helper, and fixed setup script, zero PDB or state files, and a matching SHA-256 sidecar. Gitleaks found no secrets in the public tree or staged package, and NuGet reported no vulnerable packages.
- Public pilot release evidence on 2026-08-17: prerelease `v0.4.0-pilot.1` contains exactly the win-x64 package and SHA-256 asset. The public bootstrap passed one clean install and two in-place updates in the background, created the Start Menu shortcut, preserved a synthetic external state file, kept the installed application hash stable, and removed its prior-version backup. Launch was intentionally disabled and no sharing or client mutation was performed.
- Supported-Windows SMB compatibility fix on 2026-08-18: the live CIM provider exposed SMB dialect values as decimal strings, which the symbolic-only adapter reduced to Unknown and caused fail-closed host-setup refusal. TDD RED failed eight numeric cases; GREEN passes all 27 focused SMB parser/probe tests. After the owner-authorized host minimum was hardened to SMB 3.0, the production preflight reported Computer, Managed folder, Local access, and SMB Ready on two consecutive background runs. Format verification, Release build with zero warnings/errors, all 241 automated tests, Gitleaks, and the NuGet vulnerability audit pass. SMB1 remained disabled; no share, account, folder, ACL, firewall, or file mutation occurred.
- Public compatibility release evidence on 2026-08-18: prerelease `v0.4.0-pilot.2` points to synchronized fix commit `f89c795` and contains exactly the verified 74,186,361-byte win-x64 zip plus SHA-256 sidecar. The public bootstrap updated the local installation with launch disabled; the installed app, Windows adapter, helper, and fixed setup script hash-match the published stage, the Start Menu shortcut remains, backup cleanup completed, and the installed production preflight reports Computer, Managed folder, Local access, and SMB Ready twice. No Computer Use, application UI, sharing mutation, or client mapping ran during release verification.
- Host mutation process-boundary fix on 2026-08-18: live setup exposed a UTF-8 BOM rejected by Windows PowerShell 5.1 and an inherited PowerShell 7 `PSModulePath` that blocked its signed built-in security module. Two real-process regression tests were RED before the respective fixes and GREEN after BOM-less stdin plus native module-path isolation. An owner-authorized elevated forced-rollback diagnostic traversed the complete real group/account/ACL/share/firewall/ledger plan to the intentional full-success checkpoint, then removed every diagnostic object and temporary artifact while preserving the managed folder. Format verification, Release build with zero warnings/errors, and all 243 automated tests pass. No Computer Use was used.
- Public process-boundary fix release evidence on 2026-08-18: prerelease `v0.4.0-pilot.3` points to synchronized fix commit `da967e9` and contains exactly the verified 74,186,371-byte win-x64 zip plus matching SHA-256 sidecar. The stage contains no PDB, state-like file, vulnerable dependency, or Gitleaks finding. The public bootstrap updated the local installation with launch disabled; all product assemblies, the helper, and the setup script hash-match the stage, the Start Menu shortcut remains, backup cleanup completed, and the installed production preflight reports Computer, Managed folder, Local access, and SMB Ready twice. No Computer Use or Windows sharing mutation ran during release verification.
